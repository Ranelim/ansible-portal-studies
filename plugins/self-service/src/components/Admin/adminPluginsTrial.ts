/**
 * Admin Plugins rail + page (`/self-service/admin/plugins`) and
 * Experiences “Manage plugins” buttons.
 *
 * Parked for this prototype pass — Experiences + Access Control stay.
 * Set `SHOW_ADMIN_PLUGINS` to `true` to revive the rail item, page, and CTAs.
 */
export const SHOW_ADMIN_PLUGINS = false;
