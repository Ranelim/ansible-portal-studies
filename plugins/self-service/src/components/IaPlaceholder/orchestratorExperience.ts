/**
 * Orchestrator is not a Portal experience in this prototype.
 * Keep the wizard, route, and catalog data — hide it from Bridge / Admin / switcher.
 * Set `SHOW_ORCHESTRATOR_EXPERIENCE` to `true` to revive.
 */
export const SHOW_ORCHESTRATOR_EXPERIENCE = false;
