/** Masthead only on Experiences shell branch (no Opt 1–5 purple banner). */
export const IA_BANNER_HEIGHT = 0;
export const MASTHEAD_HEIGHT = 64;
export const CHROME_TOP = IA_BANNER_HEIGHT + MASTHEAD_HEIGHT;
